# afk-bypass
AFK Bypass is a lightweight browser console script by Nanodevs that keeps AFK rewards running on sites like LemonHost even when the tab is inactive. It overrides visibility checks, blocks pause signals, and allows reward requests to continue normally. No extensions or installs—just paste and run.
